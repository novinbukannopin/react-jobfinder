import {Hero, Navbar, RecommendedJobs, TopCompany} from "../components";

const Home = () => {
    return (
        <div>
            <Navbar/>
            <Hero/>
            <RecommendedJobs/>
            <TopCompany/>
        </div>
    );
};

export default Home;
