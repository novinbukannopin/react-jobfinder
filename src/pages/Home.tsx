import {Hero, Navbar, RecommendedJobs} from "../components";

const Home = () => {
    return (
        <div>
            <Navbar/>
            <Hero/>
            <RecommendedJobs/>
        </div>
    );
};

export default Home;
